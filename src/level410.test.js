const largestOverlapOfSubstring = require("./level410");

test("", () => {
    var arr = ["abcdefg", "cdefghijklm", "ghijklmnopqr"]
    expect(largestOverlapOfSubstring(arr)).toBe();
});

test("", () => {
    var arr = ["hello", "world", "foobar", "barfoo", "he", "llo"]
    expect(largestOverlapOfSubstring(arr)).toBe();
});

test("", () => {
    var arr = ['apple', 'banana', 'orange', 'kiwi', 'strawberry']
    expect(largestOverlapOfSubstring(arr)).toBe();
});

test("", () => {
    var arr = ["abcdefg", "abcde", "abcdef", "ab", "abc"]
    expect(largestOverlapOfSubstring(arr)).toBe();
});



