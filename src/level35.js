function smallestPositiveInteger(arr){
    var sums = [];
    for(i=0; i < arr.lenght; i++){                 
        var sum = arr[i];
        for(j=i+1; j < arr.length; i++){
            sum += arr[j];
            if()
        }

    }
}